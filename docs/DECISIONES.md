# DECISIONES — Log de decisiones estructurales (append-only)

> Formato: `D-NNN · fecha · decisión · por qué (1-2 líneas)`. Nunca editar entradas pasadas; si una decisión se revierte, añadir una nueva que la sustituya referenciándola.

- **D-001** · 2026-07-10 · Plataforma modular (core común + módulos), no apps standalone. — Resuelve fragmentación, duplicación y control de datos sensibles a la vez.
- **D-002** · 2026-07-10 · Stack 100% TypeScript: React+Vite, Tailwind+shadcn/ui, NestJS, PostgreSQL+Prisma, monorepo pnpm+Turborepo. — Un solo lenguaje, estructura rígida ideal para generación con IA, equipo domina JS/TS.
- **D-003** · 2026-07-10 · Infra en AWS Lightsail 32GB/640GB en la red privada del grupo; Docker Compose sin PaaS; Nginx nativo en host; GitHub Actions→GHCR→deploy por webhook. — Alineado con la operación existente (36 Moodles); Docker por paridad CI↔prod, rollback por tag y aislamiento de runners.
- **D-004** · 2026-07-11 · BD: Lightsail managed PostgreSQL desde el día 1, plan Standard $30/mes (2GB/80GB, cifrado en reposo, modo privado). Descartados: plan $15 (sin cifrado), Aurora (costo), MongoDB (rigor relacional es ventaja para la fábrica). — Backups PITR gestionados y familiarización temprana con el entorno definitivo por $30/mes.
- **D-005** · 2026-07-10 · Integración Cowork vía plugin de organización: skill `awk-prototipo` + conector MCP remoto contra la API de la fábrica. Sin ZIPs manuales. — Normaliza la entrada y da seguimiento/mantenimiento desde Cowork.
- **D-006** · 2026-07-10 · Pipeline con spec intermedia y gates humanos (spec + PR), obligatorios ante datos sensibles, migraciones al core o complejidad alta. — La spec es el punto de encuentro negocio↔técnica y hace la generación auditable.
- **D-007** · 2026-07-11 · Trabajo por tareas independientes de Cowork con contexto vivo en el repo (CLAUDE.md + STATUS.md + este log) y selección de modelo por tipo de tarea. — Ver `docs/07-metodo-de-trabajo.md`.
- **D-008** · 2026-07-11 · Prototipo original movido a `legacy/` (referencia conceptual; se elimina al reconstruir el dashboard en Fase 1). Este repo queda limpio para alojar el monorepo. Añadido `.gitignore`; `node_modules` y `backend/.env` sacados del tracking. — Evitar que tareas futuras contaminen el desarrollo tomando el prototipo como base.
- **D-009** · 2026-07-11 · Convenciones del monorepo: paquetes compartidos consumibles desde ESM (web/Vite) y CJS (api/Nest) — `@awk/types` y `@awk/auth` compilan dual con tsup; `@awk/ui` se exporta como fuente TS (la app la transpila) con tokens de marca como theme de Tailwind v4. TypeScript fijado en 5.9.x (la 7.x nativa aún no emite `emitDecoratorMetadata`, requisito de la DI de Nest). — Fija la forma que replicará cada módulo generado.
- **D-010** · 2026-07-11 · Prisma 7 con el generador nuevo `prisma-client` (cliente TS generado en `apps/api/src/generated/prisma`, gitignored) + driver adapter `@prisma/adapter-pg`; la URL de conexión vive en `prisma.config.ts` (CLI) y en el adapter (runtime), ya no en el schema. Conexión perezosa (sin `$connect` en boot). — Sin engines Rust en generate/runtime: funciona en el sandbox de Cowork (binaries.prisma.sh bloqueado), menos piezas en CI/deploy, y es la dirección oficial de Prisma 7 (`prisma-client-js` es legacy).
- **D-011** · 2026-07-11 · Patrón auth/RBAC/manifest del core: JWT propio de plataforma (HS256, `JWT_SECRET`) emitido hoy por dev-login (solo no-producción) y mañana por el IdP, detrás de los contratos de `@awk/auth`; guards globales `JwtAuthGuard`+`RolesGuard` con decoradores `@Public()`/`@Roles()`; la regla de acceso `canAccess` es ÚNICA y compartida api↔web; cada módulo declara un `module.manifest` (schema Zod en `@awk/types`, data serializable) y el shell web construye menú y rutas desde el registro de manifests filtrando por roles. — Un solo punto de verdad para permisos y navegación; es el contrato que cada módulo generado por la fábrica debe cumplir.
- **D-012** · 2026-07-11 · PostgreSQL **18.x** en todo el pipeline (Lightsail managed, docker-compose dev, BD efímera de CI), con paridad estricta de major entre los tres. Corrige la elección inicial de 16, que se basó en un supuesto no verificado ("Lightsail va retrasado de versiones") y en docs de AWS desactualizadas; la consola real ofrece hasta 18.4. — Greenfield sin nada provisionado: el major más nuevo estable maximiza el runway de soporte (EOL nov 2030 vs nov 2028) y los upgrades de major en Lightsail son costosos; Prisma soporta PG 18 oficialmente. Los upgrades futuros de major se hacen a la vez en prod y dev/CI.
- **D-013** · 2026-07-11 · CI (`.github/workflows/ci.yml`) y patrón de imagen Docker para apps Node del monorepo. CI: un job aplica `prisma migrate deploy` contra un Postgres 18 de servicio (BD efímera, paridad D-012) y luego `pnpm exec turbo run build lint typecheck test`; un segundo job construye las imágenes de `apps/api` y `apps/web` en cada PR (sin publicar, como gate de que los Dockerfiles compilan) y las publica en GHCR con tag `sha`+`latest` solo en `main` (D-003). Patrón Docker para apps Node del workspace (usado por `apps/api/Dockerfile`, replicable para cada módulo futuro que corra en Node): instalar y compilar el workspace completo, luego `pnpm --filter=<pkg> deploy --prod --legacy <dir>` para aislar el paquete con solo sus dependencias de producción resueltas como archivos reales (no symlinks) en un directorio autocontenido que se copia tal cual a la imagen final. `apps/web` no necesita este paso (build estático servido por `nginx:alpine`). Dos gotchas de pnpm 11 detrás de esta forma (detalle y evidencia en `docs/STATUS.md`): (1) el atajo `pnpm turbo` (sin `exec`) dispara un chequeo de deps que puede reinstalar en modo `--production` a mitad de la corrida y romper la tarea — usar siempre `pnpm exec turbo`; (2) `pnpm deploy` sin `--legacy` exige `inject-workspace-packages=true` (no configurado); con `--legacy` resuelve igual de bien los paquetes `workspace:*` porque `apps/api`, `packages/types` y `packages/auth` ahora declaran `"files": ["dist"]` (si no, `pnpm deploy`/`npm pack` excluyen `dist/` por caer al `.gitignore` de la raíz al no haber `.npmignore`). — Fija el patrón que cada módulo generado por la fábrica deberá seguir para tener imagen Docker propia dentro del mismo monorepo/imagen de plataforma (ver docs/03, "unidad de deploy es la plataforma completa").
- **D-014** · 2026-07-12 · Historia de git reescrita con `git-filter-repo` para eliminar del historial completo tres directorios preexistentes a la reorganización de D-008: `backend/node_modules`, `frontend/node_modules` y `backend/uploads` (layout viejo, previo a `legacy/`). El disparador fue un `git push` real a GitHub rechazado por `pre-receive hook declined`: un blob de 170MB en `backend/uploads` excede el límite de 100MB de GitHub. `.git` bajó de 183MB a ~500KB; los 15 commits y sus mensajes se preservaron intactos (solo cambian los hashes); el contenido actual de `legacy/` (23 archivos, incluido un zip de muestra de 116KB en `legacy/backend/uploads/`) no se tocó. Como el remoto (`origin`, repo privado nuevo) nunca había recibido un push exitoso, no hizo falta force-push ni coordinar con nadie más. — Esos directorios eran basura pre-D-008 sin ningún valor de referencia (node_modules commiteado por error, zips de prueba de la app vieja); mantenerlos en el historial no aporta nada y bloqueaba por completo poder subir el repo a GitHub. Nota para cualquier tarea futura: los hashes de commit anteriores a esta fecha citados en conversaciones o STATUS.md ya no son válidos.
- **D-015** · 2026-07-12 · Docker Engine + Compose v2 instalados en el Lightsail de cómputo (32 GB, existente/compartido) desde el **repo oficial** (`get.docker.com`): `docker-ce` + `containerd` + `buildx` + `compose-plugin`. Descartados `snap docker` (confinamiento estricto rompe bind mounts fuera de rutas limitadas → problemas con volúmenes, integración con el Nginx nativo del host y runners efímeros del Agent SDK) y `apt docker.io` (paquete community, retrasado y sin plugin Compose v2). No toca Nginx/certbot/pm2 ni las apps existentes. Versiones al instalar: Engine 29.6.1, Compose v5.3.1. — Método estándar para host único; Compose v2 como plugin (`docker compose`) es lo que usarán los compose files de staging/production.
- **D-016** · 2026-07-12 · Deploy de la plataforma en el Lightsail de cómputo: `deploy/docker-compose.yml` único (sin secretos) reutilizado por los proyectos compose `awk-staging`/`awk-production` en `/opt/awkfactory/<entorno>/`, con `.env` por entorno fuera del repo (plantillas `deploy/staging.env.example` / `production.env.example`); contenedores publicados solo en `127.0.0.1` (el Nginx nativo del host es el único camino de entrada, server blocks nuevos en `deploy/nginx/`, dominios reales `staging.factory.wiloxagency.com` / `production.factory.wiloxagency.com`, certbot ya instalado). Puertos 18100-18103 (api/web × staging/producción), elegidos por ser poco comunes y **pendientes de confirmar** contra el uso real del server (nadie con acceso verificó `ss -tlnp` antes de esta tarea). Deploy automático: `.github/workflows/deploy.yml` despliega staging por SSH cuando `CI` termina en verde sobre `main`; producción se promueve a mano (`workflow_dispatch` con el `sha` validado en staging — "promoción = re-tag", D-003). Para correr `prisma migrate deploy`/seed contra la managed (privada) sin instalar Node/pnpm en el host ni clonar el repo ahí: nuevo stage `migrator` en `apps/api/Dockerfile` (reutiliza el stage `build`, que ya trae prisma CLI + tsx como devDependencies, D-013) publicado en GHCR como `awkplatform-api-migrator` con el mismo tag que `awkplatform-api`; `deploy/scripts/migrate.sh` hace `docker pull` + `docker run --rm --env-file` de esa imagen desde el Lightsail. Llave SSH dedicada `awk-deploy` (no reutiliza ninguna llave personal) autorizada solo para este deploy. — El sandbox de Cowork que preparó estos artefactos no tiene ruta de red al Lightsail (proxy/allowlist, verificado con `Network is unreachable`); la ejecución real de este runbook (`docs/runbooks/lightsail-deploy.md`) la hizo Leonardo a mano por SSH, dejando los deploys *siguientes* automatizados vía GitHub Actions.
- **D-017** · 2026-07-13 · Confirmado en el Lightsail real: staging y production de D-016 sirven por HTTPS en sus dominios, con roles de BD, migración+seed y dev-login verificados end-to-end (403 correcto en production, D-011). En el camino se corrigieron varios bugs de una sola vez que quedaron documentados en los runbooks (`docker run --env-file` no admite comillas; falta `GRANT ... ON SCHEMA public` para `_prisma_migrations`; `@prisma/adapter-pg` necesita `uselibpqcompat=true` contra el cert de Lightsail; `pnpm exec` de cualquier binario —no solo `pnpm turbo`— dispara el chequeo de deps de pnpm 11 y borra devDependencies sin TTY, usar `node_modules/.bin/<bin>` directo). El más grave: `ci.yml` construía `apps/api/Dockerfile` sin `--target`, y como el stage `migrator` (D-016) quedó *después* de `runtime` en el archivo, Docker construyó por defecto el último stage y publicó el contenido de `migrator` bajo el tag `awkplatform-api` → crash loop real en producción (el contenedor intentaba `prisma migrate deploy` en vez de levantar el servidor Nest). Corregido añadiendo `target: runtime` explícito en el job `docker` de `ci.yml`. — **Regla para todo Dockerfile futuro con más de un stage servible** (cada módulo de la fábrica probablemente lo tendrá): declarar siempre `target:` explícito en el workflow que publica la imagen; nunca depender de cuál stage queda último en el archivo.
- **D-018** · 2026-07-13 · Migración de dominio: `staging.factory.wiloxagency.com` → `staging.apps.awakelab.world` y `production.factory.wiloxagency.com` → `apps.awakelab.world`. Mismo server (Lightsail `13.38.161.213`, sin cambios), DNS de los dos subdominios nuevos ya apuntando al server antes de esta tarea. Cambiados en el repo: `deploy/nginx/{staging,production}.conf` (server_name + comentarios) y `docs/runbooks/lightsail-deploy.md` (tabla de dominios, comandos certbot/scp, checklist). El cutover real en el server (nuevos server blocks + certbot en paralelo a los viejos, verificación, y solo entonces retiro de server blocks/certificados viejos — sin downtime) queda en `docs/runbooks/migracion-dominio-2026-07.md`, pendiente de ejecución manual por Leonardo por el mismo motivo que D-016/D-017 (sandbox de Cowork sin ruta de red al Lightsail). — Alinear la plataforma con el dominio corporativo `awakelab.world` en vez de `wiloxagency.com` (dominio de otra entidad del grupo, usado solo como provisional en D-016).
- **D-019** · 2026-07-13 · Confirmado en el Lightsail real: cutover de D-018 ejecutado por Leonardo siguiendo `docs/runbooks/migracion-dominio-2026-07.md` sin downtime (server blocks + certbot nuevos en paralelo a los viejos, verificados por HTTPS/dev-login, y solo entonces retirados los server blocks y certificados de `staging.factory.wiloxagency.com`/`production.factory.wiloxagency.com`). Sin cambios en `.github/workflows/deploy.yml` ni en GitHub Secrets — el deploy automático a staging (D-016/D-017) sigue funcionando igual sobre el dominio nuevo. — Cierra la migración de dominio; `staging.apps.awakelab.world`/`apps.awakelab.world` quedan como los dominios reales y únicos de la plataforma.
- **D-020** · 2026-07-13 · Aclaración de alcance para "primer módulo ejemplar" (Fase 0, ver `docs/06-roadmap.md`): `legacy/` (carga de ZIP + dashboard de seguimiento de estado) NO es candidato a módulo ejemplar ni base de ningún módulo de producto. Es el panel de control interno de la propia fábrica — el roadmap ya lo fija como ítem de Fase 1 ("migrar el control plane actual... conservar conceptualmente el dashboard y el stepper"), coherente con D-008 ("se elimina al reconstruir el dashboard en Fase 1"). El "prototipo real sencillo" de Fase 0 debe ser un prototipo real de negocio (del tipo que un gerente construiría en Cowork, la premisa central de la fábrica), no legacy/ ni su dominio (gestión de archivos/estado). El uso de legacy/ se limita a referencia de estilo/patrón general de una app fullstack simple. — Leonardo señaló que la redacción anterior en `STATUS.md` ("ver legacy/ como referencia conceptual") era ambigua y podía llevar por error a tomar el dashboard interno de legacy/ como si fuera el candidato a módulo ejemplar, cuando en realidad esa reconstrucción ya tiene su propio lugar (Fase 1) y su propio propósito (herramienta interna, no producto de cliente).
  **Nota (misma fecha, sin nueva decisión — ya cubierto por D-005 y `docs/04-integracion-cowork.md`)**: al reconstruir ese dashboard en Fase 1, la funcionalidad de "carga de ZIP" de `legacy/` NO se replica. La integración directa Cowork↔fábrica (`submit_prototype` vía conector MCP, D-005: "sin ZIPs manuales") la vuelve innecesaria — los prototipos entran normalizados desde Cowork, no por archivo subido a mano. El dashboard de Fase 1 se reduce a **solo lectura/aprobación**: catálogo de soluciones/módulos existentes, estado del pipeline por proyecto (timeline), preview en staging, historial de cambios y auditoría (quién aprobó qué) — ya descrito en `docs/04-integracion-cowork.md` ("El dashboard"), que explícitamente indica "Quitar: la selección manual de subdominio por archivo... y los cambios de estado manuales desde la UI".
- **D-021** · 2026-07-13 · El "módulo ejemplar" de Fase 0 (patrón D-011, prototipo elegido según D-020) se construye con el mismo proceso colaborativo Claude+Leonardo usado en todo Fase 0 hasta ahora (core mínimo, CI/Docker, deploy, migración de dominio): Claude implementa (diseño, código, tests, buenas prácticas) y Leonardo supervisa/decide sobre negocio y requerimientos, ejecutando solo los pasos que de verdad requieran su Mac por falta de red del sandbox (como D-016/D-018). No lo construye Leonardo por su cuenta al margen de Cowork. "Construido a mano" (`docs/06-roadmap.md`) distingue este módulo del pipeline automatizado de la fábrica (spec intermedia + gates + generación por Agent SDK, Fase 1-3, aún no existe) — nunca implicó prescindir de asistencia de Claude. Dependencia sin resolver: Leonardo debe aportar/elegir el prototipo real de negocio antes de iniciar (Claude no lo inventa); en ausencia del pipeline de intake de `docs/04`, Leonardo cumple aquí el rol de "gerente" (necesidad de negocio) y de "revisor técnico", con Claude como implementador — un ensayo en miniatura del flujo que `docs/04` describe para cuando esté automatizado. — Razones dadas por Leonardo: velocidad (Claude es más rápido escribiendo código), coherencia con el resto del código/decisiones ya generados por Claude, escalabilidad de cambios futuros (Claude mantiene contexto completo de decisiones/convenios con mínimo margen de error) y robustez (tests, buenas prácticas y calidad de código en paralelo, mientras Leonardo se enfoca en verificación, flujos y requerimientos de negocio).
- **D-022** · 2026-07-13 · Módulo ejemplar elegido y construido (D-020/D-021): **moodle-insights**, dashboard denso de solo lectura sobre una réplica parcial de una instancia Moodle (cursos, alumnos, calificaciones). Decisiones de esta tarea:
  - **Alcance**: single-tenant — una sola instancia por `MOODLE_URL`/`MOODLE_TOKEN` (variables de entorno, patrón igual a `JWT_SECRET`), sin selector multi-instancia aunque el grupo opere 36 Moodles (decisión explícita de Leonardo para esta v1; añadir selector es extensión futura sin romper el contrato).
  - **Integración**: cliente REST propio (`apps/api/src/modules/moodle-insights/moodle-client.service.ts`) sobre `fetch` nativo de Node — sin dependencia HTTP nueva — contra 4 wsfunctions de Moodle Web Services: `core_course_get_courses`, `core_course_get_categories`, `core_enrol_get_enrolled_users` (filtrado a rol `student`) y `gradereport_user_get_grade_items` (se usa el item `itemtype="course"`, la nota total). Documentadas en `apps/api/.env.example` para que Leonardo las habilite en el servicio externo de Moodle.
  - **Disparo manual, sin cron/webhook**: el único disparador es `POST /api/moodle-insights/sync` (`@Roles('admin')`); las lecturas (`GET /summary|/courses|/students`) están abiertas a cualquier autenticado. Cada sync reemplaza el replicado completo dentro de una transacción ("full refresh" — ver comentario extenso en `schema.prisma`), adecuado al volumen esperado (una instancia, cientos de filas, no millones); si un módulo futuro necesita más escala, cambiar a upsert+diff. Cada sync queda registrado en `moodle.sync_runs` (éxito/error, contadores) y en `core.audit_events`.
  - **Esquema propio**: nuevo schema PG `moodle` (multiSchema de Prisma, junto a `core`) con `sync_runs`, `courses`, `students`, `enrollments`, `grades` — primer módulo que ejercita el patrón "cada módulo añade su propio schema PG" de `docs/02-stack.md`.
  - **Sin acceso a un Moodle real en el sandbox** (mismo motivo que D-016: sin ruta de red saliente): desarrollado con fixtures/mocks fieles a la documentación pública de Moodle Web Services y tests unitarios sobre esos fixtures (35 tests en `apps/api`, 10 en `apps/web`, ver `MoodleDashboardPage.test.tsx`). **Pendiente**: Leonardo debe validar contra un Moodle real (configurar el servicio externo, `MOODLE_URL`/`MOODLE_TOKEN`, correr un sync real) — sigue en "Bloqueos".
  - **Extensión de contrato compartido**: `NavItem` (`@awk/types`) gana un campo opcional `icon` (nombre de icono de `lucide-react`, serializable) para que el shell pinte un icono por módulo en el sidebar (petición explícita de Leonardo, estilo consola AWS). Aplicado también a `hello` (`Sparkles`) y `core-admin` (`ShieldCheck`) por consistencia — cambio aditivo, no rompe manifests existentes sin icono.
  - **Primeras dependencias de UI del platform**: `recharts` (gráficos) y `lucide-react` (iconos) añadidas a `apps/web` — quedan como el patrón a reutilizar por cualquier módulo futuro con densidad visual (dashboards tipo AWS console, la dirección visual pedida por Leonardo). El bundle de `apps/web` sube a ~704KB (aviso de Vite, no error); code-splitting queda como mejora futura, no bloqueante para este módulo.
  — Cierra el bloqueo abierto desde D-020/D-021 ("elegir prototipo real de negocio"): Leonardo lo definió como "dashboard denso con widgets y gráficos sobre datos de Moodle, con botón 'Actualizar datos' que dispara una réplica parcial vía webservices que él mismo configura" — el caso de uso que motivó explícitamente la elección del estilo visual de consola AWS para toda la plataforma (ver también nota de estilo en `docs/02-stack.md`/brand guideline).
- **D-023** · 2026-07-13 · Limitación descubierta del sandbox de Cowork sobre carpetas de usuario montadas (`/Users/leonardobarreto/projects/app-factory` tal como la ve esta tarea): por defecto el mount **bloquea `unlink()`/`rename()`** (borrar o mover archivos existentes), incluida a herramientas de bash tan comunes como `rm` de locks huérfanos de git, symlinks dentro de `node_modules`, o el output gitignored de `prisma generate` (`apps/api/src/generated/prisma`) — `pnpm install` (recrea `node_modules` completo), `prisma generate` (limpia su carpeta antes de escribir) y `git commit` con un `index.lock`/`HEAD.lock` huérfano fallan con `EPERM: operation not permitted, unlink`. Sobrescribir el CONTENIDO de un archivo existente (`cp`, o las herramientas Write/Edit) sí funciona sin pedir nada — el límite es específicamente borrar/recrear, no modificar. **Hay una salida sancionada para borrar**: la tool `allow_cowork_file_delete` (pide permiso al usuario una vez por carpeta) habilita el borrado — así se resolvieron los locks huérfanos de git que bloqueaban el commit de esta misma tarea. **Workaround para instalar dependencias nuevas o regenerar Prisma** (sin depender de esa tool, porque `pnpm install`/`prisma generate` necesitan borrar/recrear muchos archivos a la vez, no uno puntual): copiar el repo con `rsync` (excluyendo `node_modules/`, `.git/`, `.turbo/`, `src/generated/`) a un directorio fuera del mount (p. ej. `/tmp/verify/app-factory`), instalar/generar/buildear/testear ahí con total libertad, y copiar de vuelta al mount (con `cp`, nunca `mv`/`rm`+`cp`) solo los artefactos versionados que cambiaron (típicamente `pnpm-lock.yaml`) — el código fuente se edita directamente en el mount con Write/Edit desde el principio, nunca en la copia temporal. — Evita repetir el mismo diagnóstico (`EPERM`, `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`) en la próxima tarea que toque dependencias o se tope con un lock de git huérfano.
- **D-024** · 2026-07-14 · Con Fase 0 cerrada, arranca **Fase 1** (pipeline con spec intermedia + gates, docs/06) en vez de construir un segundo módulo de negocio a mano primero. `moodle-insights` (D-020/D-021/D-022) ya validó de punta a punta el patrón D-011 como replicable; un segundo módulo manual no habría añadido aprendizaje nuevo sobre ese patrón, mientras que el pipeline automatizado es el activo central de la fábrica — cuanto antes se construya, antes se puede medir su métrica de éxito real (% de módulo utilizable sin retoque manual, docs/06). Fase 1 sigue teniendo la misma dependencia que D-021 tuvo para Fase 0: necesita un prototipo real de negocio como primer caso, y Leonardo debe aportarlo — verificado en esta tarea que la "cola de prototipos existentes" que menciona `docs/06-roadmap.md` es lenguaje aspiracional, no una lista real: no existe carpeta `prototypes/`/`queue`/`backlog` en el repo ni ningún prototipo nombrado en ningún doc fuera de `moodle-insights`. — Queda pendiente de definir con Leonardo el orden concreto de arranque dentro de Fase 1 (mecánica del pipeline vs. reconstrucción del control plane) y el prototipo piloto (ver STATUS.md, "Siguiente").
- **D-026** · 2026-07-15 · **Orden de arranque de Fase 1 confirmado por Leonardo** (resuelve el pendiente que D-024 dejó abierto): secuencia (0) terminar el **frontend de `orientador-ia`** → (1) **mecánica del pipeline** con Agent SDK (spec/gate/run como modelo de datos + runner de generación, docs/04 pasos 2-6) → (2) **control plane** como UI de lectura/aprobación sobre los datos reales del pipeline. Invierte el orden literal de `docs/06-roadmap.md` (que lista "migrar el control plane" antes que el pipeline semiautomático). — El control plane es una UI *sobre* datos del pipeline (docs/04: timeline de estados, visor de spec, gates, preview links); construirlo primero obligaría a diseñar contra un modelo de datos que aún no existe (churn). El pipeline primero **define** ese modelo (proyectos/specs/gates/runs/estados), front-loadea el mayor riesgo/aprendizaje, y aprovecha que la experiencia del pipeline corrido a mano en `orientador-ia` (D-025) sigue fresca. Mismo criterio que dio buen resultado en `moodle-insights`: backend/datos primero, dashboard encima. La nota `[decisión] Arranque concreto de Fase 1` guardada en STATUS.md el 2026-07-14 queda superada por esta entrada: su "pregunta 2" (prototipo piloto) ya estaba resuelta (`orientador-ia`, D-024/D-025, backend generado y verificado) y su "pregunta 1" (orden) queda cerrada aquí.
- **D-025** · 2026-07-14 · **Gate spec aprobado** para `orientador-ia` (primer caso real de Fase 1, ver `docs/pipeline/orientador-ia/`): Leonardo aprobó spec funcional y técnica con estas resoluciones — contenido de itinerario/academias (13 sectores, precios, links `refactika.com`) se mantiene tal cual sin revisión adicional; admins de Aspasia (más de uno) comparten la misma vista de leads sin partición individual; límite de uso de la IA de **50 ejecuciones por candidato** (tope anti-abuso, identificado por email al no haber login — contador en `orientador.leads`, más rate-limit básico por IP antes del consentimiento); modelo de Claude confirmado: **Haiku**. Queda como único punto de implementación (no de negocio) confirmar si el shell soporta rutas públicas sin login sin romper la asunción D-011 de "todo detrás de RBAC" — se resuelve durante la generación. — Cierra el primer ciclo completo de spec intermedia + gate (docs/04, pasos 2-3) ejercitado con un caso real; pasa a generación (paso 4).
- **D-027** · 2026-07-15 · **Rutas públicas del shell web** (paso 0 de D-026, resuelve el punto de implementación dejado abierto en D-025): `ModuleRegistration` (`apps/web/src/modules/types.ts`) gana un campo opcional `publicRoutes` (mismo shape que `routes`, distinto de los ítems de `nav` del manifest) — un módulo que lo declara expone esas rutas SIN login y sin el `<Layout>`/sidebar del shell. `registry.ts` añade `publicModuleRoutes()` (independiente de usuario/roles, a diferencia de `accessibleModules`/`visibleNav`); `App.tsx` calcula esas rutas una sola vez (`publicRouteElements()`) y las inyecta idénticas en las tres ramas de `AppRoutes` (loading/anónimo/autenticado) para que una ruta pública responda igual sin importar el estado de la sesión. Sin cambios al manifest compartido (`@awk/types`) ni al backend: `@Public()` (D-011) ya cubría el lado API, el único hueco era el router del shell asumiendo "todo detrás de login". Primer consumidor: `orientador-ia` (`/orientador-ia`, landing+wizard del candidato) — el panel admin del mismo módulo sigue el patrón normal (`routes` + manifest con `requiredRoles: ['orientador_admin']`). — Cambio mínimo y genérico (D-025 lo pedía así, "reutilizable por cualquier módulo futuro con landing pública"): no toca RBAC, no reemplaza el patrón default, solo añade una vía de escape explícita por módulo.
- **D-028** · 2026-07-15 · **Reversión parcial de marca para el frontend público de `orientador-ia`**: contradice el punto de D-025 que daba por hecho "se rebrandea a identidad Awakelab 2026 ... se abandona la identidad visual 'Refactika' del prototipo" — Leonardo pidió explícitamente mantener Refactika en las pantallas del candidato ("el frontend debe conservar la marca de refactika, la misma de la referencia"), tras revisar 6 capturas del prototipo original y el PDF de referencia (`Itinerario_IA___Desarrollo.pdf`) y notar que se había perdido información/estructura del original. Alcance de la reversión: **solo** las páginas públicas sin login (`/orientador-ia`, `/orientador-ia/mercado`) — el panel admin (`/orientador-ia/admin`, dentro del shell AwkPlatform) sigue con la identidad Awakelab 2026 (org-level instructions), sin cambio. Implementación: fuente Manrope cargada en `index.html` con comentario explícito de scope ("SOLO para orientador-ia"), paleta propia en `refactika-theme.ts` (naranja `#FF6A00` — ya presente en `academy.color` desde el seed original, D-025 — + navy `#0B1220`) inyectada como CSS custom properties (`--rf-*`) en el elemento raíz de cada página pública, consumida por clases Tailwind con arbitrary values (`text-[var(--rf-navy)]`). Contenido reconstruido a partir de: las 6 capturas + el prototipo fuente completo hallado en `legacy/backend/uploads/1781515328337-484868779-orientadorRefactika.zip` (componentes `MarketIntel`/`SECTORS`/`DATA_SOURCES`/`REGIONAL_HUBS`/`REGIONAL_COMPANIES` de `app.js`) — landing con las 3 tarjetas del flujo, wizard de 3 pasos (meta+multi-selección de sectores estilo chat → historia/LinkedIn/CV+nivel con 3 tarjetas → resultado), entrada de voz opcional (Web Speech API, degradación silenciosa fuera de Chrome/Edge), "Comprar online" reemplazado por un toast "Próximamente" (mismo patrón para el toggle LATAM y el link "API", ninguno tiene contenido real todavía), sección libre "Inteligencia de mercado" (`/orientador-ia/mercado`, sin entrevista ni consentimiento) con datos España-only del prototipo (ofertas/salarios/% IA/ciudades/empresas/tendencias por los 13 sectores, `market-intel-data.ts`), y PDF del itinerario rediseñado (`itinerary-pdf.ts`, 3 páginas: portada con KPIs y cita destacada, troncal común de 120h, escuela recomendada + cierre con 3 CTAs) para acercarse en calidad visual al PDF de referencia. — Decisión de negocio de Leonardo como dueño del proyecto (candidato final es cliente de Grupo Aspasia/Refactika, no de Awakelab); documentada aquí porque revierte explícitamente una resolución previa de D-025.
- **D-029** · 2026-07-15 · **Mecánica del pipeline** (paso 1 de D-026, docs/04-integracion-cowork.md pasos 2-6): nueva app **`apps/factory`** (NestJS, mismo patrón que `apps/api`: Prisma 7 sin engines Rust + adapter-pg, D-010) — sistema separado de la Plataforma (docs/03: "dos sistemas separados que conviene no mezclar"), ya anticipado como app propia del monorepo en docs/02-stack.md. Decisiones concretas (confirmadas por Leonardo, todas la opción recomendada):
  - **Base de datos**: NO una instancia Postgres nueva. Misma instancia managed $30/mes (D-004), una base lógica separada (`awkfactory_staging`/`awkfactory_production`, en dev `FACTORY_DATABASE_URL` distinta de `DATABASE_URL`) — sin schemas compartidos con `core`/`moodle`/`orientador`, aislamiento por base de datos completa. Pendiente crear las bases y el rol de mínimo privilegio en el Lightsail real (mismo patrón que D-004/D-016), no bloqueante para esta tarea.
  - **Modelo de datos** (`apps/factory/prisma/schema.prisma`, migración a mano `20260715180000_factory_init` — mismo motivo que `core_init`/moodle/orientador): `Project` (estado del pipeline, `moduleSlug` único = carpeta final en la Plataforma), `Spec` (versión de la spec intermedia: `functionalContent`/`technicalContent` markdown + `complexityScore`/`sensitivityFlags`/`reuseNotes`, nunca se edita in-place — nueva versión por cada "complementar"), `Gate` (`functional`/`technical`/`pr_review`/`manager_acceptance`; `approved`/`rejected`/`changes_requested`, docs/05: las tres salidas del revisor), `Run` (una ejecución del Agent SDK: `analysis` o `generation`, con rama/PR/costo/tokens/`session_id`).
  - **Máquina de estados** (`pipeline/state-machine.ts`): transiciones válidas de `ProjectStatus` tomadas literalmente de la lista de docs/03 ("Estados del proyecto"), con dos casos añadidos no obvios: `spec_ready → analyzing` (permite revisar/regenerar la spec tras un gate "changes_requested" sin inventar un estado nuevo) y `verifying → generating` / `error → generating` (reintentos, docs/04 paso 5: "Falla → el runner itera"). Único punto de escritura de `status`: `ProjectsService.transition`, que valida contra este mapa antes de cualquier `update`.
  - **Alcance del runner — ambos runners reales** (no un stub): `AnalysisRunnerService` invoca el Agent SDK headless (`@anthropic-ai/claude-agent-sdk`, nueva dependencia — única forma real de que Claude lea el prototipo + explore módulos existentes del propio repo para antiduplicación y escriba archivos, a diferencia de la llamada estructurada simple de `orientador-claude.service.ts`) para que escriba `docs/pipeline/<slug>/{spec-funcional.md,spec-tecnica.md,meta.json}` — el runner solo relee esos archivos del disco, los guarda como `Spec` v1, abre los gates funcional+técnico y deja el proyecto en `pending_approval`. `GenerationRunnerService`, con ambos gates ya `approved` (docs/05: "nunca se genera desde una spec sin gate"), crea la rama `factory/<slug>`, corre el Agent SDK con escritura acotada por un callback `canUseTool` (el guardarraíl real; el `systemPrompt` es solo instrucción, no enforcement) a `apps/api/src/modules/<slug>/`, `apps/web/src/modules/<slug>/` y `apps/api/prisma/schema.prisma`, y — si hay red/`gh` CLI — abre la PR; sin red (frecuente en el sandbox de Cowork, D-016/D-023) el commit queda local y el proyecto igual avanza a `verifying`, documentado como limitación operativa de esta fase, no como bug.
  - **Sin cola de trabajos ni contenedores todavía**: docs/06 (Fase 1: "lanzado por un dev") vs. Fase 3 ("cola de trabajos, runners en contenedor") — los runners corren in-process, invocados por CLI.
  - **Sin servidor HTTP/dashboard todavía**: se opera 100% por CLI (`apps/factory/src/cli.ts`: `create-project`, `analyze`, `decide-gate`, `generate`, `advance`, `status`) contra `NestFactory.createApplicationContext`. El HTTP/dashboard es el paso 2 de D-026 (control plane) — se añadirán controllers sobre los mismos servicios de `PipelineModule` sin tocarlos.
  - **Sin auth propia todavía**: sin login/roles de la Fábrica en esta fase (Fase 1: un solo operador, Leonardo, doble rol gerente+revisor como en D-021) — `Gate.reviewer` es texto libre, no una FK a `core.users` (la Fábrica no comparte base de datos con la Plataforma). Se diseñará junto con el dashboard de Fase 2.
  - **Modelo de Claude**: Sonnet por defecto (`ANTHROPIC_MODEL`, configurable) — docs/07-metodo-de-trabajo.md clasifica "implementar un módulo sobre plantilla existente" como desarrollo estándar, no como decisión arquitectónica que ameritara Opus/Fable.
  - **Verificación**: `pnpm exec turbo run build lint typecheck test` en verde sobre las 6 apps/paquetes del monorepo (28/28 tests nuevos en `@awk/factory`, sin tocar ni regresionar `@awk/api` 48/48 ni `@awk/web` 29/29) — corrido en una copia temporal fuera del mount (workaround D-023: instalar dependencias nuevas), con `pnpm-lock.yaml` actualizado copiado de vuelta al mount. — Fija el activo central de la fábrica (modelo de datos del pipeline) para que la próxima tarea (control plane, paso 2 de D-026) tenga datos reales sobre los que construir, en vez de diseñar una UI contra un esquema imaginado.
- **D-030** · 2026-07-15 · **Control plane de la Fábrica** (paso 2 de D-026, docs/04 "El dashboard"): HTTP sobre `PipelineModule` + UI de seguimiento/aprobación. Tres decisiones confirmadas por Leonardo (todas la opción recomendada):
  - **La UI vive en apps/web como módulo `factory-console`** (`/factory`, `requiredRoles: ['admin']`, patrón D-011), NO como app frontend propia — cero infra nueva de frontend, reutiliza shell/auth, y es el end-state natural: en Fase 2 los gerentes verán este dashboard con sus cuentas de plataforma. Páginas: lista de proyectos y detalle con stepper del camino feliz (docs/03), visor de spec funcional/técnica por versión con decisión de gates, e historial cronológico reconstruido de specs/gates/runs (sin tabla de eventos nueva).
  - **Auth: apps/factory VALIDA el JWT de plataforma** (emitido por apps/api) compartiendo solo el secreto de firma (`JWT_SECRET`, misma env var) — la Fábrica no emite tokens propios y sigue sin compartir base de datos con la Plataforma (D-029 intacta). `FactoryAuthGuard` global (nuevo, en `apps/factory/src/control-plane/`) exige rol `admin` en toda la superficie salvo `/factory-api/health` (healthcheck del contenedor). Servidor: `src/main.ts`, prefijo `/factory-api` (puerto `FACTORY_PORT`, 3100), proxy de Vite en dev y location de Nginx en staging/production. El CLI ahora usa `CliModule` (sin HTTP/JWT) — `AppModule` quedó como raíz del servidor.
  - **La UI solo decide gates** (`POST /factory-api/gates/:id/decision`, reviewer SIEMPRE del JWT, nunca del body): `analyze`/`generate` siguen siendo comandos de CLI — son runs largos del Agent SDK y sin cola de trabajos (Fase 3, D-029) no corresponde dispararlos desde una request HTTP. Lectura: `GET /factory-api/projects` y `/projects/:id` (nuevo método aditivo `ProjectsService.list()`; el resto de servicios D-029 sin tocar). Contrato compartido en `@awk/types` (`factory*Schema`) — se comparte el contrato, no la BD.
  - **Wiring de deploy**: `apps/factory/Dockerfile` (patrón D-013, stages `runtime`+`migrator` con `target:` explícito en CI por la lección D-016), servicio `factory` en `deploy/docker-compose.yml` (puertos host 18104 staging / 18105 production), locations `/factory-api/` en ambos `.conf` de Nginx, `FACTORY_DATABASE_URL`/`FACTORY_PORT_HOST` en las plantillas `.env`, `deploy/scripts/migrate-factory.sh` e imagen `awkplatform-factory-migrator` en CI. **Bug latente de D-029 corregido de paso**: `pnpm-lock.yaml` ya tenía el importer `apps/factory` pero los Dockerfiles de api/web no copiaban su `package.json` en el stage `deps` — `pnpm install --frozen-lockfile` habría fallado en el próximo build de Docker en CI; añadido el COPY en ambos (y en el de factory).
  - **Verificación**: `pnpm exec turbo run build lint typecheck test` en verde (build/typecheck/lint 17/17 tareas; tests 8/8 paquetes — `@awk/factory` 37/37 con 9 nuevos, `@awk/web` 35/35 con 6 nuevos, `@awk/api` 48/48 sin regresiones) — corrido en copia temporal fuera del mount (D-023, deps nuevas: `@nestjs/jwt`/`@nestjs/platform-express`/`zod`/`@awk/auth`/`@awk/types` en factory), `pnpm-lock.yaml` copiado de vuelta. — Pendiente operativo (no de código): crear bases+roles `awkfactory_*` en la managed PG y añadir el bloque `/factory-api/` a los `.conf` reales del server a mano (nunca scp, incidente certbot).
- **D-031** · 2026-07-19 · **Las `decisionNotes` de los gates aprobados son parte del contrato de generación, y las desviaciones del código generado se corrigen regenerando, nunca parcheando a mano** (decisión explícita de Leonardo durante el primer encargo real `gestor-proyectos`, reafirmando docs/05 frente a la tentación pragmática): la revisión de la PR #1 detectó que la reasignación de tareas no quedó solo-admin — esa instrucción vivía en las `decisionNotes` del gate técnico y el prompt de generación solo llevaba las specs, así que el agente nunca la vio. Ante la disyuntiva "fix manual de 5 líneas vs. regenerar (~$11)", Leonardo decidió cumplir docs/05 al pie: (1) se corrigió LA FÁBRICA — `GenerationRunnerService` ahora consulta los gates `approved` de la spec e inyecta sus notas en el prompt como bloque vinculante ("si contradicen la spec, prevalecen las notas"), y el system prompt gana una cláusula de REGENERACIÓN (si el módulo ya tiene código, ajustar en vez de reescribir); (2) se regeneró por el camino formal de la máquina de estados (`pr_review → changes_requested → generating`, que ya existía en D-029 sin haberse usado) — la regeneración fue incremental (~4 min) y aplicó el fix exacto en backend y frontend con tests. Costo de la vía formal: una corrida más de API; beneficio: el canal revisor→agente queda reparado para SIEMPRE, no solo para este módulo, y el módulo sigue siendo 100% producto del pipeline (métrica de éxito de docs/06 sin asterisco). — Complementa D-029/D-030; los gaps operativos menores que el mismo encargo destapó (gates pr_review/manager_acceptance sin fila en BD, PR existente no detectada al regenerar, Run desactualizado en la salida del CLI, carrera de `prisma generate` en turbo) quedan anotados en STATUS "Siguiente" como mejoras, no decisiones.
- **D-032** · 2026-07-19 · **Primer patrón de integración cross-módulo: lectura directa solo-lectura + copia, sin tocar el módulo fuente** (decidido en los gates del segundo encargo real `focus-flow`, donde Leonardo pidió integrar la cola de tareas con `gestor-proyectos` contra la recomendación del análisis de dejarlo para después — decisión legítima de negocio que obligó a resolver el "cómo"): la spec proponía un endpoint `mis-tareas` en `gestor-proyectos`, pero el guardarraíl de generación (docs/04 paso 4) impide al runner escribir fuera de las carpetas de su módulo, y añadirlo a mano crearía drift sobre un módulo generado (contra D-031). El patrón elegido: la integración vive 100% en el módulo consumidor — su servicio lee las tablas del módulo fuente vía el cliente Prisma compartido (solo lectura, filtrado por el userId del JWT) y crea COPIAS en sus propias tablas (columna `source*Id` uuid nullable SIN FK para deduplicar; nunca referencia viva ni sincronización). El acoplamiento queda a nivel de lectura de schema ajeno, explícito en el gate técnico. Aplica a cualquier integración cross-módulo futura mientras no exista un mecanismo de contratos entre módulos.
- **D-033** · 2026-07-19 · **Enmienda de `decisionNotes` de un gate ya decidido: UPDATE firmado en BD + comentario en la PR (mecanismo provisional) — y el hallazgo de que las notas imprecisas son bugs del revisor, no de la fábrica**: en la revisión de la PR #2 (`focus-flow`) la única desviación encontrada fue causada por una nota de gate imprecisa ("status != completada" se aplicó literal a `finalizada`, sin excluir `cancelada` — el generador incluso documentó que no ampliaba el filtro sin nueva aprobación: comportamiento correcto). Para corregir por la vía D-031 (regenerar con notas como contrato) hizo falta enmendar la nota, pero `GatesService.decide` rechaza gates no-`pending` por diseño y no existe mecanismo de re-decisión. Provisional acordado: `UPDATE gates SET "decisionNotes" = ... || enmienda` directo en BD, con la enmienda FIRMADA en el propio texto (fecha, revisor, motivo) + comentario en la PR como registro público; luego `advance changes_requested` → `generate` (la regeneración leyó la nota enmendada y aplicó el fix exacto con tests, ~2 min). Queda como gap del pipeline diseñar la re-decisión/enmienda de gates de primera clase (junto a los gates pr_review/manager_acceptance sin fila en BD, mismo grupo de mejoras).
- **D-034** · 2026-07-19 · **`request_change` construido como re-entrada del pipeline existente sobre el mismo Project (modelo LIGERO), + cierre de los gaps 3/4/5 de STATUS** (docs/04 "Mantenimiento"; diseño propuesto y aprobado por Leonardo antes de implementar — las tres opciones recomendadas). Decisiones:
  - **Modelo de datos ligero, no un ciclo de estados propio por cambio**: `request_change` NO es un pipeline nuevo — es re-entrar el pipeline existente sobre el MISMO `Project`. Nueva entidad ligera `ChangeRequest` (id/projectId/requestedBy/requestText, `@@map("change_requests")`) que solo guarda el texto de la petición y quién la pidió (docs/05, "firmado por persona"); `Spec` gana `changeRequestId String?` (null = spec de intake original; con valor = mini-spec de un cambio). `Project.status` sigue siendo el ÚNICO tracker del pipeline: refleja la ITERACIÓN en curso, no la salud de lo que ya está vivo en prod (que no se toca hasta mergear+promover). Descartado el modelo pesado (máquina de estados propia por `ChangeRequest`): duplicaría state-machine y control-plane para un Fase 1 de un solo operador con una iteración activa por módulo. Migración a mano `20260719180000_change_requests` (mismo motivo que las anteriores). SIN enum `run_type` nuevo: el análisis de cambio reutiliza `analysis` y la generación `generation` — el control-plane renderiza la mini-spec como una versión más de `Spec` sin cambios en `@awk/types` ni en la UI.
  - **Re-entrada en la máquina de estados**: `deployed → analyzing` (deja de ser terminal), `staging → analyzing`, `manager_acceptance → analyzing`. Además `pr_review → rejected` y `manager_acceptance → rejected` (un cambio/módulo puede descartarse en la revisión, no solo pedir cambios) para que `GatesService.decide` sea uniforme.
  - **Análisis de cambio**: `AnalysisRunnerService.runChangeAnalysis(changeRequestId)` (comparte con `runAnalysis` el nuevo helper privado `finalizeSpec`). La fuente NO es un prototipo HTML sino el código vivo del módulo (`apps/api|web/src/modules/<slug>`) + el texto de la petición + la spec vigente como contexto; system prompt propio que exige una MINI-spec del delta (no re-derivar el módulo), escrita en `docs/pipeline/<slug>/change-<n>/`. Abre gates funcional+técnico frescos (docs/05 sigue forzando revisión si el cambio toca datos personales/core/deps). La generación se REUTILIZA sin cambios de fondo (ya soportaba rama y código existentes, D-031); único ajuste: el system prompt ahora permite editar los modelos PROPIOS del módulo en `schema.prisma` (antes solo "añadir nuevos"), necesario para cambios que tocan el schema del propio módulo. CLI: `request-change <projectId> --request "..." --requested-by x@y` (crea el CR + corre el análisis de cambio).
  - **Gap 3 — gates `pr_review`/`manager_acceptance` de primera clase + enmienda**: la generación, al dejar una PR, abre automáticamente el gate `pr_review` (antes la revisión docs/05 quedaba como comentario suelto + `advance` manual, sin fila auditable); aprobar `pr_review` (→ staging) abre el gate `manager_acceptance` sobre la misma spec. **Bug corregido**: `GatesService.decide` mandaba TODO `changes_requested` a `spec_ready`; para gates de revisión (`pr_review`/`manager_acceptance`) "complementar" debe REGENERAR (→ `changes_requested` → `generating`), no re-analizar la spec. Nuevo `GatesService.amendNotes` (CLI `amend-gate`): enmienda las notas de un gate ya decidido preservando la original + sello de auditoría, sin re-decidir ni transicionar — reemplaza el UPDATE por SQL a mano de D-033 (la regeneración lee las notas enmendadas, D-031).
  - **Gap 4 — PR existente**: en una regeneración incremental la rama `factory/<slug>` suele tener PR abierta y `gh pr create` fallaba (`prUrl` null). Ahora se reutiliza la PR existente (`gh pr view --json url`) antes de intentar crearla.
  - **Gap 5 — Run desactualizado**: `runGeneration` devolvía el objeto de CREACIÓN del run (status `running`, sin prUrl/costo). Ahora relee el run antes de devolverlo, para que el CLI imprima el estado final real.
  - **Verificación**: `@awk/factory` 58/58 tests (15 nuevos: `runChangeAnalysis`, `ChangeRequestsService`, gates de primera clase + fix + `amendNotes`, gap 4, re-entrada de estados), `tsc --noEmit` y `eslint` en verde — copia fuera del mount (D-023), cliente Prisma regenerado con `ChangeRequest`. SIN dependencias npm nuevas (`pnpm-lock.yaml` idéntico) ni cambios en `@awk/types`/apps/web. **Pendiente**: el ejercicio end-to-end contra staging (objetivo 3: `gestor-proyectos` "Desempeño por persona" solo-admin) corre en el Mac de Leonardo — el sandbox no alcanza el túnel de staging (`localhost:15432`) ni `PLATFORM_REPO_PATH`, misma frontera de infra que D-016/D-031. — Cierra el gap histórico 7 (`request_change`) y los gaps 3/4/5 del pipeline; deja la pieza de mantenimiento de docs/04 lista para ejercitarse con las observaciones reales de Leonardo.
- **D-035** · 2026-07-20 · **`request_change` ejercitado de punta a punta contra staging (valida D-034 en la práctica) — 3 bugs reales encontrados y corregidos en el camino**: se ejercitó el flujo completo de mantenimiento (docs/04) con un caso REAL en vez del backlog previsto — dos observaciones de Leonardo sobre `focus-flow` tras validarlo en staging: (1) bug visual de los interruptores de Configuración (el thumb del `Switch` sin ancla `left`, solo `translate-x`, quedaba mal posicionado en ambos estados), (2) el temporizador en curso se perdía al recargar (revertir la limitación MVP "decisión 6"). El cambio recorrió `request-change` → `runChangeAnalysis` (mini-spec del delta, fiel, sin alucinaciones) → gates funcional+técnico decididos en `/factory` de staging con notas vinculantes (persistencia server-side en tabla nueva `focus.timer_state` fila-por-usuario filtrada por `userId` sin excepción admin, el cliente sigue siendo la autoridad del conteo, `PUT` solo en eventos discretos —nunca por tick—, fase vencida→00:00 en pausa sin autocompletar, `localStorage` descartado) → generación incremental (PR, ~10 min, $3.43, build/test 11/11) → revisión docs/05 (Claude, sin desviaciones) → gate `pr_review` aprobado → migración a mano `20260720100000_focus_timer_state` en la integración humana → merge PR #3 → migrar `awkplatform_staging` → **validado en vivo por Leonardo** (toggles correctos + timer que sobrevive a recargar/reiniciar) → gate `manager_acceptance` aprobado. Los gates de primera clase `pr_review`/`manager_acceptance` de D-034 funcionaron por primera vez en un caso real.
  - **Bug 1 (fábrica) — el análisis de cambio dejaba `change-<n>/` vacío**: el system prompt de `runChangeAnalysis` listaba los archivos con ruta parcial (`change-<n>/spec-funcional.md`); el agente escribía en `<repo>/change-<n>/` (raíz del repo), fuera del `writableRoot` absoluto, y `canUseTool` denegaba TODA escritura — el run terminaba "con éxito" pero sin archivos (a diferencia del análisis de intake, cuya lista sí usaba la ruta completa). Fix (`[fix]`): ruta COMPLETA `docs/pipeline/<slug>/change-<n>/` en el prompt + prohibición explícita de rutas relativas, `mkdir -p` del `specDir` antes de correr el agente (el `change-<n>/` no existe en el primer cambio), y el guard extraído a `isWriteAllowed` (pura, testeable) que resuelve rutas relativas contra `cwd` y matchea por frontera de segmento. Regresión cubierta.
  - **Bug 2 (fábrica) — gap-4 reutilizaba PRs MERGED/CLOSED**: `findExistingPrUrl` hacía `gh pr view <rama> --json url` y devolvía cualquier PR de la rama sin mirar su estado; en un `request_change` sobre un módulo YA mergeado, la rama `factory/<slug>` se reabre para el cambio pero su PR de intake está MERGED, y se "reutilizaba" esa PR cerrada dejando los commits del cambio sin PR revisable. Fix (`[fix]`): consultar `--json url,state` y reutilizar solo si `state === 'OPEN'`; si no, crear una PR nueva. (En este ejercicio la PR #3 se abrió a mano porque el fix no estaba vigente en esa corrida; el `Run.prUrl` de ese run quedó apuntando a la #2 mergeada — artefacto cosmético documentado, no actualizado por SQL porque `psql` no está instalado en el Mac.)
  - **Bug 3 (plataforma, `[core]`) — `apiFetch` fallaba con body vacío**: `focus-flow GET /timer-state` es el PRIMER endpoint nullable de la plataforma; cuando devuelve `null`, NestJS envía una respuesta 200 VACÍA (no el literal JSON `null`) y `res.json()` lanzaba en el navegador ("The string did not match the expected pattern" en WebKit — de ahí el críptico "No se pudo cargar el módulo"). Fix: `apiFetch` usa `res.json().catch(() => null)`. **Lección**: un primer intento con `res.text()` rompió los 40 tests del módulo (sus mocks de fetch exponen `.json()` pero no `.text()`) — cambiar un helper compartido obliga a respetar el estilo de mock existente; corregir localmente antes de pushear. Body vacío → `null` (un schema `.nullable()` lo acepta, uno no-nullable da un ZodError claro). Test de regresión nuevo `apps/web/src/lib/api.test.ts`.
  - **Aprendizajes operativos repetidos**: `~/migrate.sh`/`~/migrate-factory.sh` viven en el Lightsail, NO en el Mac (correrlos requiere SSH al server); Prisma 7 NO auto-carga `.env` (usar `node --env-file=...` o inline la URL, si no cae al fallback local — pasó dos veces); el checkout runner debe estar en `main` limpio, con `pnpm install` tras un pull con lockfile cambiado y su branch `factory/<slug>` local stale borrada para recrearla desde main. — Cierra el objetivo 3 de D-034; `request_change` queda validado de punta a punta. Backlog restante sin tocar: `gestor-proyectos` "Desempeño por persona" solo-admin y `hourlyDistribution` de focus-flow en UTC.
- **D-036** · 2026-07-20 · **Diseño de arranque de Fase 2 — Integración Cowork (docs/06, D-005, docs/04): sesión de solo diseño, aprobado por Leonardo antes de escribir código (patrón D-026/D-029/D-030; las cuatro opciones recomendadas)**. Tres restricciones que condicionan el diseño: (1) NO hay SSO — docs/05 pide "OAuth contra el SSO" pero la única identidad hoy es dev-login (deshabilitado en producción), así que hace falta auth interina (esta entrada documenta explícitamente esa desviación temporal de docs/05); (2) D-030 sigue vigente: analyze/generate no se disparan por HTTP sin cola (Fase 3), y el contenedor `factory` desplegado no tiene checkout del repo ni `ANTHROPIC_API_KEY` — el análisis server-side es infra aparte, no un endpoint más; (3) el sandbox de Cowork SÍ alcanza `apps.awakelab.world` por HTTPS (verificado en sesiones previas) → un remote MCP bajo `/factory-api` es alcanzable desde Cowork. Decisiones:
  - **El conector MCP `awkfactory` vive DENTRO de `apps/factory`** (no app nueva del monorepo): endpoint MCP Streamable HTTP montado en el mismo Nest (`/factory-api/mcp`) con `@modelcontextprotocol/sdk`, reutilizando los servicios de `PipelineModule` tal cual. Cero contenedor/puerto/bloque Nginx/job de CI nuevos; una `apps/mcp` solo aportaría aislamiento de proceso que ya da el guard.
  - **Auth interina: PAT por gerente** (Bearer revocable). Nueva tabla `factory_actors` en la BD de la fábrica (email, rol `gerente|admin`, hash del token, revocación) — sin FK a `core.users`, coherente con D-029/D-030 (se comparte contrato, no BD). Tokens emitidos por Leonardo vía CLI/console. Cumple el "firmado por persona" de docs/05 sin construir un IdP; cuando exista el SSO se migra PAT→OAuth sin tocar el contrato de tools. El plugin de organización configura el conector remoto con `Authorization: Bearer ${AWKFACTORY_TOKEN}`. **Riesgo a verificar en el incremento A contra Cowork real** (Mac, frontera D-016/D-023): que el `.mcp.json` del plugin expanda env vars en headers; si no, fallback a evaluar (p. ej. token en config del conector).
  - **Contrato de tools v1** (todo sobre `/factory-api`, transporte-agnóstico — los mismos endpoints sirven a la console y al MCP): `list_modules` → `GET /modules` nuevo (deriva de `Project`: slug, estado, resumen de spec — antiduplicación desde Cowork); `submit_prototype` → `POST /submissions` nuevo (name + HTML + `prototype.manifest.json` validado con Zod desde `@awk/types`, nuevo `prototypeManifestSchema`) crea `Project` con `sourceType=cowork_prototype` (enum ya existente, D-029) + fila en tabla nueva `prototype_submissions` (manifest jsonb + fuente en texto + submittedBy); `get_project_status` → `GET /projects/:id` con scope por rol (gerente ve SOLO sus proyectos — `requestedBy` = email del actor del PAT; admin ve todos); `request_change` → `POST /change-requests` nuevo (usa `ChangeRequestsService` D-034 tal cual); `approve_spec` → `POST /gates/:id/decision` existente + modelo de roles en `GatesService`: el rol `gerente` decide solo gates `functional`/`manager_acceptance` de SUS proyectos; `technical`/`pr_review` siguen siendo solo-admin.
  - **`submit_prototype` NO dispara el análisis en el incremento A**: deja el proyecto en `received` con la fuente en BD; el análisis sigue lanzado por dev vía CLI (control de costo + D-030), y el CLI aprende a leer la fuente desde `prototype_submissions` (reemplaza el `--source-ref` manual para proyectos `cowork_prototype`; la fuente se materializa a disco solo al correr analyze, en ruta ya legible — **guardarraíl de generación sin cambios**, es un guard de escritura). El gerente ve "recibido, pendiente de análisis" vía `get_project_status`. El análisis server-side async llega en el incremento C (checkout de solo lectura + `ANTHROPIC_API_KEY` en el contenedor + lock de concurrencia simple, sin cola) — el análisis es barato ($0.7–1.1 según los encargos reales) y docs/06 dice que "siempre corre"; la generación ($2–11) sigue lanzada por dev hasta Fase 3.
  - **Orden de piezas / incrementos**: **A** (primero): `prototypeManifestSchema` en `@awk/types` + endpoints nuevos + `factory_actors`/PAT + MCP server embebido — verificable con tests+curl en el sandbox, prueba real desde Cowork en el Mac; **B**: plugin de organización (skill `awk-prototipo` + conector) en el marketplace privado — la skill guía el prototipado (identidad Awakelab 2026, preguntas de negocio de docs/04, sensibilidad por entidad de docs/05) y termina llamando a `submit_prototype`; **C**: análisis server-side async; **D**: dashboard v2 (preview links, vista de manifest/sensibilidad, aprobación funcional en lenguaje de negocio) — al final porque su valor para gerentes reales depende del SSO; mientras tanto el gerente opera desde Cowork y Leonardo desde `/factory`. Razonamiento conector-primero: la skill sin conector produce manifests sin destino (seguiría habiendo intermediario manual) y el dashboard v2 sin gerentes con login es UI para un solo usuario que ya tiene `/factory`; el conector es la única pieza que elimina el CLI del camino gerente→fábrica.
  - **Impacto en modelo de datos (D-029): solo aditivo** — tablas nuevas `prototype_submissions` y `factory_actors`; `Project.sourceType=cowork_prototype` ya estaba anticipado. Sin cambios en `Spec`/`Gate`/`Run` ni en la máquina de estados. — Deja el incremento A listo para implementarse en la próxima sesión.
- **D-037** · 2026-07-20 · **Incremento A de Fase 2 CONSTRUIDO (ejecuta D-036 tal como se diseñó; esta entrada fija las decisiones de implementación que D-036 dejaba abiertas)**. (1) **PATs**: prefijo `awkf_` para distinguirlos del JWT en el mismo header Bearer; token aleatorio de 32 bytes + hash SHA-256 con índice único (no bcrypt: no hay password que fortalecer, y el hash simple permite lookup directo); `email` NO único en `factory_actors` — reemitir crea fila nueva y REVOCA las activas previas del mismo email (un token vigente por actor, filas viejas conservadas con `revokedAt` para auditoría); 401 idéntico para PAT inexistente y revocado. (2) **El modelo de roles vive en los servicios del pipeline, no en el guard ni en los controllers** (`assertProjectVisibleToActor` + parámetro opcional `actor` en `ProjectsService.list/getFullStatus`, `ChangeRequestsService.create` y `GatesService.decide`): transporte-agnóstico — REST y MCP comparten la misma regla, y el CLI (sin actor) queda sin scope como hasta ahora. `GET /modules` deliberadamente SIN scope: su función es la antiduplicación. (3) **MCP stateless**: un `McpServer` + transporte nuevos por request (sin sesiones), `enableJsonResponse`, GET/DELETE→405; las excepciones HTTP de los servicios se devuelven como resultado de tool con `isError` y el motivo (accionable para Claude), nunca como error de protocolo. (4) **Materialización de fuente**: `analyze` escribe la submission en `docs/pipeline/<slug>/source/{prototype.html,prototype.manifest.json}` (dentro del writableRoot ya existente; el guardarraíl no cambia — es guard de escritura) y busca la submission ANTES de transicionar (sin fuente no queda Run huérfano); el prompt del análisis pide verificar/elevar la sensibilidad declarada del manifest (docs/05: nunca rebajarla). (5) `submit_prototype` crea Project+submission en transacción con `sourceRef=db://prototype_submissions/<id>` y responde 409 accionable si el slug existe (sugiere request_change/list_modules). Verificado fuera del mount: `@awk/factory` 101/101 (43 nuevos, incl. el MCP ejercitado con Client+InMemoryTransport del SDK), api/web/types sin regresiones. Dep nueva: `@modelcontextprotocol/sdk` ^1.29.0. Gotcha de sandbox: binaries.prisma.sh da 403 → `PRISMA_SCHEMA_ENGINE_BINARY` a un stub destraba `prisma generate`. — Pendiente en el Mac/deploy (D-016): migración a staging, primer PAT, smoke curl del MCP y la prueba real desde Cowork (riesgo de env vars en headers del `.mcp.json`, D-036).
