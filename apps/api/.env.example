# Copiar a .env para desarrollo local. NUNCA commitear .env (ver D-008 / incidente Mongo).
# Dev local: Postgres en Docker. Prod: Lightsail managed PostgreSQL en modo privado (D-004).
DATABASE_URL="postgresql://awk:awk@localhost:5432/awkplatform?schema=core"
PORT=3000
